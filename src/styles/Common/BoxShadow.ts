import { StyleSheet } from "react-native";
export const BoxShadowStyle = StyleSheet.create({
  container: {
    elevation: 8,
    shadowOffset: { height: 2, width: 0 },
    shadowOpacity: 0.1,
    shadowRadius: 6,
  },
});
